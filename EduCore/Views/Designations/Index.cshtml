@model IEnumerable<EduCore.Models.Designation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DesignationName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsTeaching)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DisplayOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.DesignationName)
            </td>
            <td>
                @Html.DisplayFor(model => item.ShortName)
            </td>
            <td>
                @Html.DisplayFor(model => item.IsTeaching)
            </td>
            <td>
                @Html.DisplayFor(model => item.DisplayOrder)
            </td>
            <td>
                @Html.DisplayFor(model => item.IsActive)
            </td>
            <td>
                @Html.DisplayFor(model => item.CreatedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DesignationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DesignationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DesignationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
